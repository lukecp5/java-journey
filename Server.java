public class Server {
    double ram = 0;
    String cpu = "";
    double storage = 0;

    /**
     * Represents a server with specified RAM, CPU, and storage capacity.
     */
    public Server(double ram, String cpu, double storage) {
        this.ram = ram;
        this.cpu = cpu;
        this.storage = storage;
    }
}
